import React from 'react';
import UserForm from '../components/form/UserForm';

const UserFormPage = () => {
	return (
		<div className="page">
			<UserForm />
		</div>
	);
};

export default UserFormPage;
